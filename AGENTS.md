# AGENTS.md

이 저장소에서 작업하는 Claude, Gemini, Codex 및 기타 AI 에이전트는 반드시 이 문서를 참조하고 따른다.

## 기본 소통 원칙

- 모든 문서 작성과 사용자와의 소통은 한국어로 진행한다.
- 코드, 명령어, 파일명, API 이름처럼 원문 표기가 필요한 경우에는 예외적으로 영어 또는 원문 표기를 사용할 수 있다.

## AI 생성 파일 명명 규칙

- AI가 새로 생성하는 파일은 최종 확장자 바로 앞에 `.ai`를 포함해야 한다.
- 파일명 형식은 `[파일명].ai.[확장자]`를 따른다.
- 예시:
  - `report.ai.md`
  - `config.ai.json`
  - `helper.ai.ts`
  - `design.ai.png`

## 적용 범위

- 이 규칙은 AI가 새로 만드는 문서, 코드, 설정 파일, 이미지, 기타 산출물에 적용한다.
- 사용자가 직접 제공한 기존 파일을 수정하는 경우에는 기존 파일명을 유지할 수 있다.
- 사용자가 명시적으로 다른 파일명을 지정한 경우에는 사용자 지시를 우선하되, 가능한 경우 `.ai` 명명 규칙을 제안한다.

## 사람 작성 문서 보호

- 사람이 작성한 문서는 사용자가 특별히 지시하지 않는 이상 절대 수정하지 않는다.
- 수정 요청이 모호한 경우에는 문서를 변경하기 전에 수정 범위와 의도를 확인한다.

## 변경 이력 기록

- 코드 수정이나 문서 수정이 실질적으로 이루어진 경우에는 `History.md` 파일에 변경 이력을 추가한다.
- 변경 이력은 `[날짜-시간] 수정 요약 내용` 형식을 따른다.
- 날짜와 시간은 작업 시점의 로컬 시간을 기준으로 기록한다.

## 변경 최소화 원칙

- 코드 수정이나 기능 구현은 기존 맥락을 먼저 파악한 뒤 외과수술처럼 최소한의 변경으로 진행한다.
- 요청 범위를 벗어난 불필요한 리팩토링, 코드 삭제, 파일 구조 변경은 진행하지 않는다.

## Git 커밋 원칙

- 사용자가 "커밋해" 또는 이에 준하는 커밋 요청을 하면 CLI를 통해 Git 커밋을 진행한다.
- 커밋 전에는 마지막 커밋 이후 현재까지의 변경사항을 확인하고 요약한다.
- 커밋 메시지는 확인한 변경사항 요약을 바탕으로 작성한다.
- 커밋에는 요청된 작업과 직접 관련된 변경사항만 포함한다.
- 커밋 과정에서 예상치 못한 변경사항이 발견되면 임의로 되돌리지 않고 사용자에게 확인한다.
